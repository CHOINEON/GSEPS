server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;  # 이 부분이 SPA 라우팅을 위한 핵심 설정
    }
}